<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Product</title>
</head>
<body>
    <h1>Edit Product</h1>
    <form action="/products/<%=product._id%>?_method=PUT" method="POST">
        <label for="name">Product Name</label>
        <input type="text" name="name" id="name" value="<%=product.name%>"placeholder="product name"/><br/>
        
        <label for="price">Product Price</label>
        <input type="number" id="price" name="price" value="<%=product.price%>" placeholder="price"/><br/>
        
        <label for="category">Product Category</label>
        <select type="text"name="category" id="category" value="<%=product.category%>" placeholder="category">
            <% for(let category of categories) {%>
                <option value="<%=category%>"><%=category%></option>
                <% } %>
            <!-- <option value="fruit" <%=product.category === 'fruit' ? 'selected' : '' %>>Fruit</option>
            <option value="vegetable" <%=product.category === 'vegetables' ? 'selected' : '' %>>Vegetable</option>
            <option value="dairy" <%=product.category === 'dairy' ? 'selected' : '' %>>Dairy</option> -->
        </select><br/>
        <button>Submit</button>
        <a href="/products/<%=product._id%>">Cancel</a>

    </form>
</body>
</html>